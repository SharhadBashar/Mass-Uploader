# Mass-Uploader
C# code using Sharepoint API to automatically populate Sharepoint Lists with large amount of data
C# files were created into .exe files
RunMe.ps1 is the powershell script that takes all the user inputs and runs the executable files
